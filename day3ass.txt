Create a Standard View (active_loans_view):

Write a view that joins department and employees to display the depart name, employee name for all currently employee

Create a Materialized View (emp_dept_view):

Create a materialized view that aggregates how many employee 

Create a Standard View (active_employees):

Write a view that joins employee and department to display the department name, employee name ,bdate all currently active loans.

Create an Audit Table (employee_audit):

Columns: audit_id (SERIAL PRIMARY KEY), emplooyee_id (INT), name (VARCHAR), changed_at (TIMESTAMP DEFAULT CURRENT_TIMESTAMP).

Write a Trigger Function (log_employee_deletion):

Write a trigger function using RETURNS TRIGGER that automatically logs an entry into employee_audit whenever a employee is deleted from the employee table, capturing the deleted employee and the action 'DELETE'.
